original data is in the dataset folder.  

Models stored in model folder are result of churn.ipynb, which may take some time to complete the modeling, because it contains cross-validation and hyperparameter tuning.  

churn-v1 is a quicker and simpler version without cross-validation and hyperparameter tuning.
